# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.20

# Pydantic
pydantic>=2.11.0
pydantic-settings>=2.9.0

# ── LLM: Groq (replaces Ollama) ──────────────────────────────────────────────
groq>=0.9.0
langchain-groq>=0.3.0

# ── Vector Store: Qdrant Cloud (replaces Elasticsearch) ──────────────────────
qdrant-client>=1.9.0

# ── Embeddings (local, sentence-transformers) ─────────────────────────────────
# torch is installed separately in Dockerfile with CPU-only wheels
sentence-transformers>=3.4.0
transformers>=4.51.0

# ── LangChain Agent ───────────────────────────────────────────────────────────
langchain>=0.3.30
langchain-community>=0.3.30
langchain-classic>=1.0.0

# ── PDF Processing ────────────────────────────────────────────────────────────
PyMuPDF>=1.25.0
pdfplumber>=0.11.0
pypdf>=5.4.0

# ── Text Processing ───────────────────────────────────────────────────────────
nltk>=3.9.0
beautifulsoup4>=4.13.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.1.0
aiofiles>=24.1.0
httpx>=0.28.0
loguru>=0.7.0
tenacity>=9.1.0
nest-asyncio>=1.6.0
python-jose[cryptography]>=3.5.0
passlib[bcrypt]>=1.7.4

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.26.0
